import React from 'react';

const BlankPage = () => {
  return (
    <div>
      A list of clinics with tables and a date selector.  The number would show the total patients waiting to be seen.
      Tabs, waiting, booked, DNA
    </div>
  
  );
};

export default BlankPage;